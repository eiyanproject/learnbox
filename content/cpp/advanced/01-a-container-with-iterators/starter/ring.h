#ifndef RING_H
#define RING_H

#include <array>
#include <cstddef>
#include <iterator>

template <typename T, std::size_t N>
class Ring {
    // storage, head_, size_ ... and a const_iterator
};

#endif
