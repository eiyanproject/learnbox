public class Booking {

    // enum Status, record BookingRecord... see the lesson: the record is
    // called Booking too, so put it and the enum below this holder class.

    public static void main(String[] args) {
    }
}
