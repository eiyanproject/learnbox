/// "Hello, <name>!"
pub fn greeting(name: &str) -> String {
    todo!("build the greeting for {name}")
}

/// The title between two lines of '=' the same length as the title.
pub fn banner(title: &str) -> String {
    todo!("frame {title}")
}
